<UserControl x:Class="CandyBrowser.Windows.Views.HistoryPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="12,8" Background="#F0F0F0">
            <Grid>
                <TextBlock Text="历史记录" FontSize="14" FontWeight="SemiBold" Foreground="#333"/>
                <Button Content="清除全部" HorizontalAlignment="Right"
                        Click="ClearAllHistory_Click"
                        Background="Transparent" BorderThickness="0" Foreground="#FF6B9D"
                        FontSize="11" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Search -->
        <TextBox Grid.Row="1" x:Name="SearchBox" Margin="8,8,8,0"
                 Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                 KeyDown="SearchBox_KeyDown"
                 Padding="8,6" FontSize="13"/>

        <!-- History List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
            <ItemsControl x:Name="GroupedHistoryList" ItemsSource="{Binding GroupedHistory}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,8">
                            <Border Padding="8,6" Background="#F5F5F5">
                                <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold" Foreground="#666"/>
                            </Border>
                            <ListBox ItemsSource="{Binding Entries}"
                                     Background="Transparent" BorderThickness="0">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="8,6" Cursor="Hand"
                                                MouseLeftButtonDown="HistoryItem_MouseLeftButtonDown"
                                                MouseRightButtonDown="HistoryItem_MouseRightButtonDown"
                                                Tag="{Binding}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"
                                                               FontSize="13" Foreground="#333"/>
                                                    <TextBlock Text="{Binding Url}" TextTrimming="CharacterEllipsis"
                                                               FontSize="11" Foreground="#666"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="2" FontSize="11" Foreground="#999"
                                                           VerticalAlignment="Center" Margin="8,0,0,0"
                                                           Text="{Binding LastVisit, StringFormat='{}{0:HH:mm}'}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="3" Padding="8,4" Background="#F0F0F0">
            <TextBlock Text="就绪" FontSize="11" Foreground="#666"/>
        </Border>
    </Grid>
</UserControl>
