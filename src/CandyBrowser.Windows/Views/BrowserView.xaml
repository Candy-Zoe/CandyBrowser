<UserControl x:Class="CandyBrowser.Windows.Views.BrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- WebView2 -->
        <Grid x:Name="WebViewContainer" Grid.Row="0">
            <wv2:WebView2 x:Name="WebView"
                          NavigationStarting="WebView_NavigationStarting"
                          NavigationCompleted="WebView_NavigationCompleted"
                          WebMessageReceived="WebView_WebMessageReceived"
                          SourceChanged="WebView_SourceChanged"
                          CoreWebView2InitializationCompleted="WebView_CoreWebView2InitializationCompleted"/>

            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" Background="White" Visibility="Visible">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="🍬" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="CandyZoe浏览器" FontSize="16" Foreground="#666"
                               HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <ProgressBar x:Name="LoadingBar" IsIndeterminate="True" Width="200"
                                 Height="4" Margin="0,20,0,0" Foreground="#FF6B9D"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="#F0F0F0" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="8,2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Grid.Column="0" Text="就绪" FontSize="12" Foreground="#666"/>
                <TextBlock x:Name="ZoomText" Grid.Column="1" Text="100%" FontSize="12" Foreground="#666" Margin="20,0,0,0"/>
            </Grid>
        </Border>

        <!-- Context Menu -->
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="后退" Click="GoBack_Click"/>
                <MenuItem Header="前进" Click="GoForward_Click"/>
                <MenuItem Header="刷新" Click="Refresh_Click"/>
                <Separator/>
                <MenuItem Header="在新标签页中打开" Click="OpenInNewTab_Click"/>
                <Separator/>
                <MenuItem Header="复制链接" Click="CopyLink_Click"/>
                <MenuItem Header="开发者工具" Click="DevTools_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</UserControl>
