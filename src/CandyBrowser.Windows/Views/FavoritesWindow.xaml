<Window x:Class="CandyBrowser.Windows.Views.FavoritesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="收藏夹" Height="500" Width="600"
        WindowStartupLocation="CenterOwner" Background="White">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="收藏夹" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Grid.Column="0" Padding="8,4" FontSize="12" TextChanged="SearchBox_TextChanged"/>
            <Button Grid.Column="1" Content="添加当前页" Margin="8,0,4,0" Click="AddCurrentBtn_Click" Padding="8,4"/>
            <Button Grid.Column="2" Content="新建文件夹" Click="NewFolderBtn_Click" Padding="8,4"/>
        </Grid>

        <TreeView x:Name="FavoritesTree" Grid.Row="2"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="打开" Click="OpenBtn_Click" Width="80" Margin="0,0,8,0"/>
            <Button Content="删除" Click="DeleteBtn_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
