<Window x:Class="CandyBrowser.Windows.Views.DownloadManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CandyBrowser.Windows.ViewModels"
        mc:Ignorable="d"
        Title="下载管理器 - CandyZoe浏览器" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <Grid>
                <StackPanel>
                    <TextBlock Text="下载管理器" FontSize="20" FontWeight="Bold"
                               Foreground="{DynamicResource TextBrush}"/>
                    <TextBlock Text="{Binding DownloadCountText}" FontSize="13"
                               Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button Content="打开下载文件夹" Click="OpenDownloadFolder_Click"
                            Padding="8,4" Margin="0,0,8,0"/>
                    <Button Content="清除已完成" Click="ClearCompleted_Click"
                            Padding="8,4" Background="#FF6B9D" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <!-- Download List -->
        <ListBox Grid.Row="1" x:Name="DownloadList"
                 ItemsSource="{Binding Downloads}"
                 Background="Transparent" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:DownloadItemViewModel}">
                    <Border Padding="12,10" Margin="0,0,0,4"
                            Background="{DynamicResource SidebarBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                            CornerRadius="6">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Title Row -->
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding FileName}"
                                           FontSize="13" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"
                                           Foreground="{DynamicResource TextBrush}"/>
                                <TextBlock Grid.Column="1" Text="{Binding StatusText}"
                                           FontSize="11" VerticalAlignment="Center"
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                            </Grid>

                            <!-- Progress Bar -->
                            <ProgressBar Grid.Row="1" Height="6" Margin="0,8,0,0"
                                         Value="{Binding ProgressPercentage}"
                                         Background="{DynamicResource BorderBrush}"
                                         Foreground="#FF6B9D"/>

                            <!-- Info Row -->
                            <Grid Grid.Row="2" Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding ProgressText}"
                                           FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock Grid.Column="1" Text="{Binding SpeedText}"
                                           FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            </Grid>

                            <!-- Action Buttons -->
                            <StackPanel Grid.Row="3" Orientation="Horizontal"
                                        HorizontalAlignment="Right" Margin="0,8,0,0">
                                <Button Content="📂" ToolTip="打开文件" Click="OpenFile_Click"
                                        Tag="{Binding}" Visibility="{Binding IsCompleted}"
                                        Style="{StaticResource ActionButtonStyle}" FontSize="14"/>
                                <Button Content="📁" ToolTip="打开文件夹" Click="OpenFolder_Click"
                                        Tag="{Binding}" Visibility="{Binding IsCompleted}"
                                        Style="{StaticResource ActionButtonStyle}" FontSize="14" Margin="4,0,0,0"/>
                                <Button Content="⏸" ToolTip="暂停" Click="PauseResume_Click"
                                        Tag="{Binding}" Visibility="{Binding IsInProgress}"
                                        Style="{StaticResource ActionButtonStyle}" FontSize="14"/>
                                <Button Content="▶️" ToolTip="继续" Click="PauseResume_Click"
                                        Tag="{Binding}" Visibility="{Binding IsPaused}"
                                        Style="{StaticResource ActionButtonStyle}" FontSize="14"/>
                                <Button Content="✕" ToolTip="取消/删除" Click="CancelRemove_Click"
                                        Tag="{Binding}"
                                        Style="{StaticResource ActionButtonStyle}" FontSize="14" Margin="4,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Empty State -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="📥" FontSize="48" HorizontalAlignment="Center"/>
            <TextBlock Text="暂无下载任务" FontSize="16" HorizontalAlignment="Center"
                       Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,12,0,0"/>
            <TextBlock Text="下载的文件将显示在这里" FontSize="13" HorizontalAlignment="Center"
                       Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="2" Padding="8,6" Background="{DynamicResource SidebarHeaderBackgroundBrush}"
                CornerRadius="0,0,6,6" Margin="0,8,0,0">
            <TextBlock Text="{Binding StatusText}" FontSize="11"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
        </Border>
    </Grid>
</Window>
